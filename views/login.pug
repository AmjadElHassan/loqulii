extends layouts/login-layout.pug

block content
    .loginContainer 
        h1 Login
        form(action="", method="post")
            //- input(type="text", name="logUsername", placeholder="username or email", value=logUsername required="")
            //- input(type="password", name="logPassword", placeholder="password", required="")

            input(type="text", name="logUsername", placeholder="username or email", value="amjeezy" required="")
            input(type="password", name="logPassword", placeholder="password",value="Vigeto22!" required="")

            span.errorMessage #{errorMessage}
            input(type="submit", value="Login")
        a(href="/register") New around here? Register here!