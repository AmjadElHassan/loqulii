extends layouts/main-layout.pug

block content
    .chatPageContainer 
        .chatTitleBar 
            label(for="userSearchTextBox") To: 
            #selectedUsers
                input#userSearchTextBox(type="text", name="userSearchTextBox", placeholder="type name of user")
        .resultsContainer
        button#createChatButton(disabled) create Chat
